docs:comandos principais de git
git init: inicia o repositório local
git remote add: vincula o repositório local ao remoto
git add: adiciona em stage os arquivos mencionados(. para todos os arquivos)
git commit: commita no histórico local suas mudanças em stage
git push: envia para o repositório remoto
